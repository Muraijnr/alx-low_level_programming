#include <stdio.h>

/**
 * main - prints whatsmyname
 * @argc: number of arguments
 * @argv: the arguments
 * Return: 0
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[argc - argc]);
	return (0);
}
