#include "main.h"
/**
 * _memset - fills memory with a constant bite
 * @s: pointer
 * @b: constant bite b
 * @n: number of bites
 * Return: pointer s
 */
char *_memset(char *s, char b, unsigned int n)
{
	int a;
	int b;

	a = 0;
	b = 0;
	while (a )
}
